apiVersion: v1
kind: Service
metadata:
  name: {{ include "kpilot-ai.fullname" . }}
  labels:
    {{- include "kpilot-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kpilot-ai.selectorLabels" . | nindent 4 }}
